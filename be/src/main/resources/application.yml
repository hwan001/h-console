spring:
  config:
    import: optional:file:.env[.properties]
  application:
    name: hconsole
  redis:
    host: localhost
    port: 6379
    # password: 1234
    timeout: 2s

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    org:
      springframework:
        web: DEBUG
        security: DEBUG
        hibernate: DEBUG

hasura:
  graphql:
    endpoint: ${HASURA_ENDPOINT}
    admin-secret: ${HASURA_SECRET}

grpc:
  server:
    port: 50051